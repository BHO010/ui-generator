# Set to "true" to use the simulated AI response instead of a live endpoint
VITE_USE_MOCK_API=true

# Base URL for the live AI endpoint (used when VITE_USE_MOCK_API=false)
VITE_API_BASE_URL=http://localhost:3000/api